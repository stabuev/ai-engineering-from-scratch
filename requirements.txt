# requirements.txt — latest / flexible install (lower bounds).
# For an exact, reproducible, tested environment use requirements.lock instead:
#   uv pip install -r requirements.lock      (or: pip install -r requirements.lock)
# Verified against Python 3.12 + torch 2.7.1 + numpy 1.26.4 (the from-scratch core
# the Phase 0-3 lessons were run against).

numpy>=1.24
matplotlib>=3.7
jupyter>=1.0
torch>=2.0
torchvision>=0.15
torchaudio>=2.0
transformers>=4.30
datasets>=2.14
tokenizers>=0.13
accelerate>=0.20
scikit-learn>=1.3
pandas>=2.0
pillow>=10.0
librosa>=0.10
soundfile>=0.12
tiktoken>=0.5
anthropic>=0.25
openai>=1.0
